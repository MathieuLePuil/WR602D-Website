{# templates/history/index.html.twig #}

{% extends 'base.html.twig' %}

{% block title %}Historique{% endblock %}

{% block body %}
{#    <h1>Historique des PDFs</h1>#}

{#    {% if pdfs is empty %}#}
{#        <p>Vous n'avez aucun PDF.</p>#}
{#    {% else %}#}
{#        <ul>#}
{#            {% for pdf in pdfs %}#}
{#                <li>{{ pdf.title }} - </li>#}
{#            {% endfor %}#}
{#        </ul>#}
{#    {% endif %}#}

    <a href="{{ path('generate_pdf') }}" class="absolute m-5 inline-flex items-center justify-center leading-normal no-underline pb-1 text-black font-sans font-bold text-sm uppercase hover:text-neutral-700 focus:outline-none focus:ring-2 focus:ring-inset focus:ring-neutral-500 transition group">
        Retourner à la génération de PDF
        <svg
                class="icon icon-tabler icon-tabler-arrow-up-right"
                width="20"
                height="20"
                viewBox="0 0 24 24"
                stroke-width="2"
                stroke="currentColor"
                fill="none"
                stroke-linecap="round"
                stroke-linejoin="round"
        ><path stroke="none" d="M0 0h24v24H0z" fill="none"></path><path
                    d="M17 7l-10 10"></path><path d="M8 7l9 0l0 9"></path>
        </svg>
        <span
                class="absolute bottom-0 left-0 w-full h-0.5 bg-neutral-700 origin-bottom-right transform transition duration-200 ease-out scale-x-0 group-hover:scale-x-100 group-hover:origin-bottom-left"
        ></span>
    </a>

    <div class="min-h-screen flex flex-col items-center justify-around">
        <h1 class="font-bold text-4xl my-20">Historique des PDF</h1>
        <div class="flex items-center justify-center bg-white px-6 md:px-60 mb-20">
            <div class="space-y-6 border-l-2 border-dashed">
                {% for pdf in pdfs %}
                    <div class="relative w-full">
                        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" class="absolute -top-0.5 z-10 -ml-3.5 h-7 w-7 rounded-full text-blue-500">
                            <path fill-rule="evenodd" d="M2.25 12c0-5.385 4.365-9.75 9.75-9.75s9.75 4.365 9.75 9.75-4.365 9.75-9.75 9.75S2.25 17.385 2.25 12zm13.36-1.814a.75.75 0 10-1.22-.872l-3.236 4.53L9.53 12.22a.75.75 0 00-1.06 1.06l2.25 2.25a.75.75 0 001.14-.094l3.75-5.25z" clip-rule="evenodd" />
                        </svg>
                        <div class="ml-6">
                            <h4 class="font-bold text-blue-500">{{ pdf.title }}</h4>
                            <a href="{{ pdf.link }}" class="mt-2 max-w-screen-sm text-sm text-gray-500 hover:underline">{{ pdf.link }}</a>
                            <span class="mt-1 block text-sm font-semibold text-blue-500">{{ pdf.createdAt|date('d/m/Y H:i') }}</span>
                        </div>
                    </div>
                {% endfor %}
            </div>
        </div>
    </div>
{% endblock %}
